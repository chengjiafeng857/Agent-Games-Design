# Prompt Generation Tool Dependencies
# Install: pip install -r requirements.txt
# Or run directly: uv run generate_prompts.py --help

pyyaml>=6.0                # YAML parsing for character specs
typer>=0.12.0              # Modern CLI framework
python-dotenv>=1.0.0       # Load API keys from .env file

# Optional: For Stage 2 LLM prompt refinement
openai>=1.0.0              # OpenAI API client (GPT-4/5 with web search)

# Optional: For Stage 4 image generation with Gemini 3 Pro Image Preview
google-genai>=1.0.0        # Google Gemini API client (new SDK)
Pillow>=10.0.0             # Image handling
